# Multi-stage build for the React (Vite) client.
# The `npm install` + `npm run build` steps in this template WERE verified
# in the sandbox this was authored in (both completed successfully).

FROM node:22-alpine AS build
WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . .
RUN npm run build

FROM nginx:alpine AS runtime
COPY --from=build /app/dist /usr/share/nginx/html
EXPOSE 80
