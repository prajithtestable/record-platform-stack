image: mcr.microsoft.com/dotnet/sdk:8.0

pipelines:
  default:
    - step:
        name: Build API
        script:
          - dotnet build RecordsApi/RecordsApi.csproj -c Release
    - step:
        name: Build client
        image: node:22
        script:
          - cd ClientApp
          - npm ci
          - npm run build
